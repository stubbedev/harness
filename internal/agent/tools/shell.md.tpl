A persistent `{{ .Shell }}` terminal shared by the whole conversation, used the way a person uses one. `command` is what you type: at the prompt it is a command line and runs (Enter implied); while a program is running, answers and keystrokes go to that program, so write `\n` or `<enter>` where a person would press it. Named keys go in angle brackets: `<escape>`, `<tab>`, `<backspace>`, `<delete>`, `<up>`/`<down>`/`<left>`/`<right>`, `<home>`, `<end>`, `<pageup>`, `<pagedown>`, `<f1>`–`<f12>`, `<ctrl+c>` or any `<ctrl+letter>`; anything else in brackets is typed literally.

A command line typed while a command that reads no input is running is never dropped into the terminal blind: it is queued, and the shell runs it, in the order typed, the next time it is back at its prompt - poll empty to wait for that. If a program is waiting for an answer and typing would queue instead, send the answer with an explicit `<enter>` so it goes to the program directly. Polling a command that is still running streams its new output in chunks; its completion arrives as the exit code. Never spend a call on a bare `sleep` - it is refused: an empty call already waits for the next event, and waiting on a condition belongs in the command that checks it (a watch or a poll loop). A call returns when the process exits (with its exit code), stops for input, takes over the screen (a rendered {{ .DefaultCols }}x{{ .DefaultRows }} frame instead of a stream), or goes idle. An empty call waits for the next of those. `reset` kills a wedged session and opens a fresh one, losing its state; last resort.

cwd, variables, activated environments and held credentials carry across calls; a result ends with `<cwd>…</cwd>` only when the shell moved. Output over {{ .MaxOutputLength }} characters keeps a head and a long tail.
{{- if .ModernTools }}
Installed here; use these instead of their POSIX defaults: {{ .ModernTools }}. Reach for them first and fall back to the POSIX tool only when the modern one cannot do the job.
{{- end }}
