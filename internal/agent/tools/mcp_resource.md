Read the resources an MCP server exposes, as opposed to calling its tools. `action: list` gives a server's resource URIs, `action: read` returns one as text.

Both the server name and the URI have to be ones you actually have, from a `list` or from the server's own instructions. Guessing either fails.
