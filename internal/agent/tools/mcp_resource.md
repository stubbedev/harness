Read the resources an MCP server exposes, as opposed to calling its tools. `action: list` gives a server's resource URIs, `action: read` returns one as text. Both the server name and the URI must come from a `list` or the server's own instructions; guessing fails.
