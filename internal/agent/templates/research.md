Research a question across the web with a sub-agent that searches, follows links and synthesises an answer with sources. Slower and costlier than `fetch` and `web_search`: use those for one page or one search, this when the answer is spread over several pages.
