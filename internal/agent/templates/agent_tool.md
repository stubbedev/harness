Launch a new agent to handle a task. The "task" type is read-only (glob, grep, ls, view) for searching when you're not confident of finding the right match on the first try; other types are specialized agents for a particular kind of task. Check the `subagent_type` parameter's list of available specialized agents and prefer one that matches the task's description over the generic "task" type.
